// Library which contains printf()
#include <stdio.h>

// main() is the beginning of code execution
int main()  {
    // The printf() is a library function that sends formatted output to the screen
    printf("Hello world!\n");
    return 0;
}

// Compile with: `gcc -Wall -Wextra -o HelloWorld HelloWorld.c`
// Run with: `HelloWorld`